# ── 运行时日志 ──
*.log
nul

# ── Python 缓存 ──
__pycache__/
*.pyc
*.db

# ── 运行时生成的缓存（云端会自动刷新重建） ──
*_cache.json
photo_cache.json
automation_cache.json
app_cache.json
xray_cache.json
watcher_cache.json
trend_cache.json
metric_cache.json
quality_cache.json
feishu_todo_cache.json
todo_data.json
dashboard_meta.json

# ── 本地密钥/令牌目录 ──
data/
.env

# ── 临时/测试脚本 ──
_t.py

# ── 第三方二进制工具（不入库） ──
cloudflared.exe
